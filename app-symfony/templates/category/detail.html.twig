{% extends 'base.html.twig' %}

{% block title %}Détail Catégorie{% endblock %}

{% block content %}
<div class="px-4 py-8 max-w-screen-md mx-auto bg-white shadow rounded border border-gray-200 space-y-6">
    <h2 class="text-2xl font-bold text-[#3B4C66]">📂 {{ category.categoryName }}</h2>

    <div class="mt-4">
        <h3 class="text-xl font-semibold mb-2 text-[#3B4C66]">📚 Modules associés</h3>
        <div class="overflow-auto max-h-96 border rounded">
            <table class="min-w-full text-left text-sm">
                <thead class="bg-[#3B4C66] text-white sticky top-0">
                    <tr>
                        <th class="px-4 py-2">Nom du module</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    {% for module in category.modules %}
                        <tr class="hover:bg-gray-100">
                            <td class="px-4 py-2"><a href="{{ path('detail_module', {'id': module.id}) }}" class="text-[#3B4C66] font-semibold hover:underline hover:text-[#4F627E] transition">{{ module.mudleName }}</a></td>
                        </tr>
                    {% else %}
                        <tr>
                            <td class="px-4 py-2 italic text-gray-500">Aucun module lié à cette catégorie.</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
