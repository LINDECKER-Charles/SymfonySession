{% extends 'base.html.twig' %}

{% block title %}Détail Catégorie{% endblock %}

{% block content %}
<div class="px-4 py-8 max-w-screen-md mx-auto bg-white shadow rounded border border-gray-200 space-y-6">
    <h2 class="text-2xl font-bold text-[#3B4C66]">📂 {{ category.categoryName }}</h2>

    <div class="mt-6">
        <h3 class="text-xl font-semibold text-[#3B4C66] mb-4">📚 Modules liés</h3>

        <div class="overflow-auto max-h-[40vh] border rounded shadow">
            <table class="w-full text-left border-collapse">
                <thead class="bg-[#3B4C66] text-white">
                    <tr>
                        <th class="p-3">Nom du module</th>
                    </tr>
                </thead>
                <tbody class="text-gray-700">
                    {% for module in category.modules %}
                        <tr class="border-t hover:bg-gray-50">
                            <td class="p-3">
                                <a href="{{ path('detail_module', {'id': module.id}) }}" class="text-[#3B4C66] font-semibold hover:underline hover:text-[#4F627E] transition">
                                    {{ module.mudleName }}
                                </a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td class="p-3 italic text-gray-500">Aucun module lié à cette catégorie.</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
