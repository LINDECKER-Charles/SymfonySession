{% extends 'base.html.twig' %}

{% block title %}Créer une catégorie{% endblock %}

{% block content %}

{# Formulaire d'ajout d'un category #}
<div class="max-w-xl mx-auto mt-10 bg-white p-6 rounded shadow border border-gray-200">
    <h1 class="text-2xl font-bold text-[#3B4C66] mb-4">➕ Nouvelle Catégorie</h1>

    {{ form_start(form) }}

        <div class="mb-4">
            {{ form_label(form.categoryName, 'Nom de la catégorie', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
            {{ form_widget(form.categoryName, {'attr': {'class': 'mt-1 block w-full border border-gray-300 rounded px-3 py-2 shadow-sm focus:outline-none focus:ring-[#3B4C66]'}}) }}
            {{ form_errors(form.categoryName) }}
        </div>

        <button type="submit" class="bg-[#3B4C66] hover:bg-[#2c3b52] text-white font-semibold px-6 py-2 rounded transition">
            💾 Enregistrer
        </button>

    {{ form_end(form) }}
</div>
{% endblock %}
