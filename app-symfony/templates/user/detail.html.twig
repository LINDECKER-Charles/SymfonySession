{% extends 'base.html.twig' %}

{% block title %}Profil de {{ user.name }}{% endblock %}

{% block content %}
<div class="max-w-4xl mx-auto bg-white rounded shadow p-6 mt-8 space-y-6">
    <h2 class="text-2xl font-bold text-[#3B4C66] mb-4">👤 Profil de {{ user.name }}</h2>

    <div class="grid md:grid-cols-2 gap-4 text-gray-700">
        <p><strong>Email :</strong> {{ user.email }}</p>
        <p><strong>Sexe :</strong> {{ user.sex }}</p>
        <p><strong>Ville :</strong> {{ user.city }}</p>
        <p><strong>Code Postal :</strong> {{ user.cp }}</p>
        <p><strong>Adresse :</strong> {{ user.adress }}</p>
        <p><strong>Date de naissance :</strong> {{ user.birth|date('d/m/Y') }}</p>
    </div>

    <div>
        <h3 class="text-xl font-semibold mt-4 mb-2 text-[#3B4C66]">📅 Sessions encadrées</h3>
        <ul class="divide-y divide-gray-200">
            {% for session in sessions %}
                <li class="py-2">
                    <a href="{{ path('detail_session', {'id': session.id}) }}" class="text-blue-600 hover:underline">
                        {{ session.sessionName }}
                    </a>
                    – Du {{ session.startDate|date('d/m/Y') }} au {{ session.endDate|date('d/m/Y') }}
                </li>
            {% else %}
                <li>Aucune session encadrée.</li>
            {% endfor %}
        </ul>
    </div>
</div>
{% endblock %}
